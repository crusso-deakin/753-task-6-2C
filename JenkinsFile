pipeline {
    agent any

    environment {
        DIRECTORY_PATH = 'C:/Path'
        TESTING_ENVIRONMENT = 'Christian Testing Environment'
        PRODUCTION_ENVIRONMENT = 'Christian Enviroment'
        RECIPIENT_EMAIL = 's223446607@deakin.edu.au'
    }

    stages{
        stage('Build'){
            steps{
                echo "Fetch the source code from the directory path specified by the environment variable: $DIRECTORY_PATH"
                echo "Build the code using Maven"
                echo "Command to be used: sh 'mvn test'"
            }
        }
        stage('Unit and Integration Tests'){
            steps{
                echo "Unit tests"
                echo "Integration tests"
            }
            post{
                success{
                    mail to: "${RECIPIENT_EMAIL}",
                    subject: "Build status email",
                    body: """<p>${currentBuild.currentResult}: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]' - Test Stage</p>
                                <p>Check console output at <a href="${env.BUILD_URL}">${env.JOB_NAME} [${env.BUILD_NUMBER}]</a></p>""",
                    attachLog: true
                }

            }
        }
        stage('Code Analysis'){
            steps{
                echo "Check the quality of the code"
            }
        }
        stage('Security Scan'){
            steps{
                echo "Scan the code"
            }
            post{

            }
        }
        stage('Deploy to Staging'){
            steps{
                echo "deploy to staging"
            }
        }
        stage('Integration Tests on Staging'){
            steps{
                echo "tests on stagin"
            }
            post{

            }
        }
        stage('Deploy to Production'){
            steps{
                echo "deploy to production"
            }
        }
    }
}